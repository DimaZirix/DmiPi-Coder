/**
 * The agent core: an embeddable coding-agent engine with no user interface. Front-ends connect
 * through the three channels (in, out, HIL); everything the agent can do comes from plugins.
 * Dependencies are well-known libraries only, each approved by the project owner first.
 */
package com.dmipi.coder.core;
