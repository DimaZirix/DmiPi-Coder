package com.dmipi.coder.core.domain.hil;

/** How many options a question's answer selects. */
public enum QuestionKind {

    /** Pick exactly one. */
    OPTION_LIST,

    /** Tick one or more. */
    CHECKBOX_LIST
}
